package com.nexomc.nexoproxy.glyphs

/**
 * Velocity-only listener removed in the BungeeCord rewrite.
 * Glyph synchronization now happens through PluginMessage events in listeners/GlyphMessageListener.
 */
class GlyphListener
